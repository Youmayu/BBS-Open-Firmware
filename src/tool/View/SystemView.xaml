<UserControl x:Class="BBSFW.View.SystemView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="680"
             d:DesignWidth="920">

	<UserControl.Resources>
		<BooleanToVisibilityConverter x:Key="BoolToVis" />

		<Style x:Key="SectionStyle" TargetType="{x:Type Border}">
			<Setter Property="Background" Value="{DynamicResource SurfaceElevatedBrush}" />
			<Setter Property="BorderBrush" Value="{DynamicResource BorderBrush}" />
			<Setter Property="BorderThickness" Value="1" />
			<Setter Property="CornerRadius" Value="8" />
			<Setter Property="Padding" Value="20" />
			<Setter Property="Margin" Value="0 0 0 16" />
		</Style>

		<Style x:Key="DescriptionStyle" TargetType="{x:Type TextBlock}">
			<Setter Property="Foreground" Value="{DynamicResource MutedTextBrush}" />
			<Setter Property="TextWrapping" Value="Wrap" />
			<Setter Property="Margin" Value="0 3 18 0" />
		</Style>
	</UserControl.Resources>

	<ScrollViewer VerticalScrollBarVisibility="Auto"
				  HorizontalScrollBarVisibility="Disabled">
		<StackPanel>
			<TextBlock Text="Pro system settings"
					   FontSize="24"
					   FontWeight="SemiBold" />
			<TextBlock Text="Detailed controller settings."
					   Foreground="{DynamicResource MutedTextBrush}"
					   TextWrapping="Wrap"
					   Margin="0 5 0 20" />

			<Grid>
				<Grid.ColumnDefinitions>
					<ColumnDefinition Width="*" />
					<ColumnDefinition Width="18" />
					<ColumnDefinition Width="*" />
				</Grid.ColumnDefinitions>

				<StackPanel Grid.Column="0">
					<Border Style="{StaticResource SectionStyle}">
						<StackPanel>
							<TextBlock Text="Power and limits" FontSize="18" FontWeight="SemiBold" Margin="0 0 0 14" />

							<Grid Margin="0 0 0 14">
								<Grid.ColumnDefinitions>
									<ColumnDefinition Width="*" />
									<ColumnDefinition Width="100" />
								</Grid.ColumnDefinitions>
								<StackPanel>
									<TextBlock Text="Max current" FontWeight="SemiBold" />
									<TextBlock Style="{StaticResource DescriptionStyle}" Text="Main current ceiling in amps. Higher values mean more torque and heat." />
								</StackPanel>
								<TextBox Grid.Column="1" Text="{Binding ConfigVm.MaxCurrentAmps, UpdateSourceTrigger=PropertyChanged}" />
							</Grid>

							<Grid Margin="0 0 0 14">
								<Grid.ColumnDefinitions>
									<ColumnDefinition Width="*" />
									<ColumnDefinition Width="100" />
								</Grid.ColumnDefinitions>
								<StackPanel>
									<TextBlock Text="Current ramp" FontWeight="SemiBold" />
									<TextBlock Style="{StaticResource DescriptionStyle}" Text="How fast current can rise. Lower values feel softer and reduce drivetrain shock." />
								</StackPanel>
								<TextBox Grid.Column="1" Text="{Binding ConfigVm.CurrentRampAmpsSecond, UpdateSourceTrigger=PropertyChanged}" />
							</Grid>

							<Grid Margin="0 0 0 14">
								<Grid.ColumnDefinitions>
									<ColumnDefinition Width="*" />
									<ColumnDefinition Width="100" />
								</Grid.ColumnDefinitions>
								<StackPanel>
									<TextBlock Text="Max battery voltage" FontWeight="SemiBold" />
									<TextBlock Style="{StaticResource DescriptionStyle}" Text="Upper battery voltage reference used by the firmware." />
								</StackPanel>
								<TextBox Grid.Column="1" Text="{Binding ConfigVm.MaxBatteryVolts, UpdateSourceTrigger=LostFocus}" />
							</Grid>

							<Grid Margin="0 0 0 14">
								<Grid.ColumnDefinitions>
									<ColumnDefinition Width="*" />
									<ColumnDefinition Width="100" />
								</Grid.ColumnDefinitions>
								<StackPanel>
									<TextBlock Text="Low voltage cutoff" FontWeight="SemiBold" />
									<TextBlock Style="{StaticResource DescriptionStyle}" Text="Battery protection threshold. Match this to the pack chemistry and series count." />
								</StackPanel>
								<TextBox Grid.Column="1" Text="{Binding ConfigVm.LowCutoffVolts, UpdateSourceTrigger=PropertyChanged}" />
							</Grid>

							<Grid>
								<Grid.ColumnDefinitions>
									<ColumnDefinition Width="*" />
									<ColumnDefinition Width="100" />
								</Grid.ColumnDefinitions>
								<StackPanel>
									<TextBlock Text="Max speed" FontWeight="SemiBold" />
									<TextBlock Style="{StaticResource DescriptionStyle}" Text="Hard current cutoff endpoint for speed limiting." />
								</StackPanel>
								<TextBox Grid.Column="1" Text="{Binding ConfigVm.MaxSpeedKph, UpdateSourceTrigger=PropertyChanged}" Visibility="{Binding ConfigVm.UseMetricUnits, Converter={StaticResource BoolToVis}}" />
								<TextBox Grid.Column="1" Text="{Binding ConfigVm.MaxSpeedMph, UpdateSourceTrigger=PropertyChanged}" Visibility="{Binding ConfigVm.UseImperialUnits, Converter={StaticResource BoolToVis}}" />
							</Grid>
						</StackPanel>
					</Border>

					<Border Style="{StaticResource SectionStyle}">
						<StackPanel>
							<TextBlock Text="Pedal assist" FontSize="18" FontWeight="SemiBold" Margin="0 0 0 14" />

							<Grid Margin="0 0 0 14">
								<Grid.ColumnDefinitions>
									<ColumnDefinition Width="*" />
									<ColumnDefinition Width="110" />
								</Grid.ColumnDefinitions>
								<StackPanel>
									<TextBlock Text="Start delay" FontWeight="SemiBold" />
									<TextBlock Style="{StaticResource DescriptionStyle}" Text="Crank movement before assist starts. Lower starts sooner; higher can prevent accidental starts." />
								</StackPanel>
								<ComboBox Grid.Column="1" ItemsSource="{Binding ConfigVm.PasStartDelayOptions}" SelectedItem="{Binding ConfigVm.PasStartDelayDegrees}" />
							</Grid>

							<Grid Margin="0 0 0 14">
								<Grid.ColumnDefinitions>
									<ColumnDefinition Width="*" />
									<ColumnDefinition Width="110" />
								</Grid.ColumnDefinitions>
								<StackPanel>
									<TextBlock Text="Stop delay" FontWeight="SemiBold" />
									<TextBlock Style="{StaticResource DescriptionStyle}" Text="How long assist continues after pedaling stops." />
								</StackPanel>
								<TextBox Grid.Column="1" Text="{Binding ConfigVm.PasStopDelayMilliseconds, UpdateSourceTrigger=PropertyChanged}" />
							</Grid>

							<Grid Margin="0 0 0 14">
								<Grid.ColumnDefinitions>
									<ColumnDefinition Width="*" />
									<ColumnDefinition Width="110" />
								</Grid.ColumnDefinitions>
								<StackPanel>
									<TextBlock Text="Keep current" FontWeight="SemiBold" />
									<TextBlock Style="{StaticResource DescriptionStyle}" Text="Current retained after cadence reaches the target for the assist level." />
								</StackPanel>
								<TextBox Grid.Column="1" Text="{Binding ConfigVm.PasKeepCurrentPercent, UpdateSourceTrigger=PropertyChanged}" />
							</Grid>

							<Grid>
								<Grid.ColumnDefinitions>
									<ColumnDefinition Width="*" />
									<ColumnDefinition Width="110" />
								</Grid.ColumnDefinitions>
								<StackPanel>
									<TextBlock Text="Keep-current cadence" FontWeight="SemiBold" />
									<TextBlock Style="{StaticResource DescriptionStyle}" Text="Cadence point where keep-current behavior begins." />
								</StackPanel>
								<TextBox Grid.Column="1" Text="{Binding ConfigVm.PasKeepCurrentCadenceRpm, UpdateSourceTrigger=PropertyChanged}" />
							</Grid>
						</StackPanel>
					</Border>

					<Border Style="{StaticResource SectionStyle}">
						<StackPanel>
							<TextBlock Text="Display" FontSize="18" FontWeight="SemiBold" Margin="0 0 0 14" />

							<Grid Margin="0 0 0 14">
								<Grid.ColumnDefinitions>
									<ColumnDefinition Width="*" />
									<ColumnDefinition Width="150" />
								</Grid.ColumnDefinitions>
								<StackPanel>
									<TextBlock Text="Display type" FontWeight="SemiBold" />
									<TextBlock Style="{StaticResource DescriptionStyle}" Text="P850C uses standard behavior. 860C ignores display work-mode writes to avoid forced ECO/Sport behavior." />
								</StackPanel>
								<ComboBox Grid.Column="1" ItemsSource="{Binding ConfigVm.DisplayTypeOptions}" SelectedItem="{Binding ConfigVm.Display, UpdateSourceTrigger=PropertyChanged}" />
							</Grid>

							<Grid>
								<Grid.ColumnDefinitions>
									<ColumnDefinition Width="*" />
									<ColumnDefinition Width="150" />
								</Grid.ColumnDefinitions>
								<StackPanel>
									<TextBlock Text="Walk-mode data" FontWeight="SemiBold" />
									<TextBlock Style="{StaticResource DescriptionStyle}" Text="What is shown in the speed field while walk mode is active." />
								</StackPanel>
								<ComboBox Grid.Column="1" ItemsSource="{Binding ConfigVm.WalkModeDataDisplayOptions}" SelectedItem="{Binding ConfigVm.WalkModeDataDisplay, UpdateSourceTrigger=PropertyChanged}" />
							</Grid>
						</StackPanel>
					</Border>
				</StackPanel>

				<StackPanel Grid.Column="2">
					<Border Style="{StaticResource SectionStyle}">
						<StackPanel>
							<TextBlock Text="Throttle" FontSize="18" FontWeight="SemiBold" Margin="0 0 0 14" />

							<Grid Margin="0 0 0 14">
								<Grid.ColumnDefinitions>
									<ColumnDefinition Width="*" />
									<ColumnDefinition Width="110" />
								</Grid.ColumnDefinitions>
								<StackPanel>
									<TextBlock Text="Start voltage" FontWeight="SemiBold" />
									<TextBlock Style="{StaticResource DescriptionStyle}" Text="Throttle voltage where input begins. Raise this if the throttle creeps at rest." />
								</StackPanel>
								<TextBox Grid.Column="1" Text="{Binding ConfigVm.ThrottleStartVoltageMillivolts, UpdateSourceTrigger=PropertyChanged}" />
							</Grid>

							<Grid Margin="0 0 0 14">
								<Grid.ColumnDefinitions>
									<ColumnDefinition Width="*" />
									<ColumnDefinition Width="110" />
								</Grid.ColumnDefinitions>
								<StackPanel>
									<TextBlock Text="End voltage" FontWeight="SemiBold" />
									<TextBlock Style="{StaticResource DescriptionStyle}" Text="Throttle voltage treated as full input." />
								</StackPanel>
								<TextBox Grid.Column="1" Text="{Binding ConfigVm.ThrottleEndVoltageMillivolts, UpdateSourceTrigger=PropertyChanged}" />
							</Grid>

							<Grid Margin="0 0 0 14">
								<Grid.ColumnDefinitions>
									<ColumnDefinition Width="*" />
									<ColumnDefinition Width="110" />
								</Grid.ColumnDefinitions>
								<StackPanel>
									<TextBlock Text="Start current" FontWeight="SemiBold" />
									<TextBlock Style="{StaticResource DescriptionStyle}" Text="Initial throttle current as a percent. Lower values make starts gentler." />
								</StackPanel>
								<TextBox Grid.Column="1" Text="{Binding ConfigVm.ThrottleStartCurrentPercent, UpdateSourceTrigger=PropertyChanged}" />
							</Grid>

							<Grid Margin="0 0 0 14">
								<Grid.ColumnDefinitions>
									<ColumnDefinition Width="*" />
									<ColumnDefinition Width="150" />
								</Grid.ColumnDefinitions>
								<StackPanel>
									<TextBlock Text="Global speed limit" FontWeight="SemiBold" />
									<TextBlock Style="{StaticResource DescriptionStyle}" Text="Optional throttle-specific speed limit behavior." />
								</StackPanel>
								<ComboBox Grid.Column="1" ItemsSource="{Binding ConfigVm.ThrottleGlobalSpeedLimitOptions}" SelectedItem="{Binding ConfigVm.ThrottleGlobalSpeedLimit, UpdateSourceTrigger=PropertyChanged}" />
							</Grid>

							<Grid>
								<Grid.ColumnDefinitions>
									<ColumnDefinition Width="*" />
									<ColumnDefinition Width="110" />
								</Grid.ColumnDefinitions>
								<StackPanel>
									<TextBlock Text="Global speed percent" FontWeight="SemiBold" />
									<TextBlock Style="{StaticResource DescriptionStyle}" Text="Percent of configured max speed used when global throttle speed limiting is enabled." />
								</StackPanel>
								<TextBox Grid.Column="1" Text="{Binding ConfigVm.ThrottleGlobalSpeedLimitPercent, UpdateSourceTrigger=PropertyChanged}" />
							</Grid>
						</StackPanel>
					</Border>

					<Border Style="{StaticResource SectionStyle}">
						<StackPanel>
							<TextBlock Text="Sensors and accessories" FontSize="18" FontWeight="SemiBold" Margin="0 0 0 14" />

							<CheckBox Content="Use speed sensor" IsChecked="{Binding ConfigVm.UseSpeedSensor, UpdateSourceTrigger=PropertyChanged}" Margin="0 0 0 10" />
							<TextBlock Style="{StaticResource DescriptionStyle}" Text="Required for road speed and speed limiting." Margin="0 0 0 14" />

							<CheckBox Content="Use shift sensor" IsChecked="{Binding ConfigVm.UseShiftSensor, UpdateSourceTrigger=PropertyChanged}" IsEnabled="{Binding ConfigVm.IsShiftSensorSupported}" Margin="0 0 0 10" />
							<TextBlock Style="{StaticResource DescriptionStyle}" Text="Momentarily reduces power during shifts." Margin="0 0 0 14" />

							<CheckBox Content="Use walk mode" IsChecked="{Binding ConfigVm.UsePushWalk, UpdateSourceTrigger=PropertyChanged}" Margin="0 0 0 10" />
							<TextBlock Style="{StaticResource DescriptionStyle}" Text="Allows low-speed walk assist from the display." Margin="0 0 0 14" />

							<CheckBox Content="Pretension chain" IsChecked="{Binding ConfigVm.UsePretension, UpdateSourceTrigger=PropertyChanged}" IsEnabled="{Binding ConfigVm.UseSpeedSensor}" Margin="0 0 0 10" />
							<TextBlock Style="{StaticResource DescriptionStyle}" Text="Applies a very small current above the cutoff speed to keep drivetrain slack out." Margin="0 0 0 14" />

							<Grid Margin="0 0 0 14">
								<Grid.ColumnDefinitions>
									<ColumnDefinition Width="*" />
									<ColumnDefinition Width="110" />
								</Grid.ColumnDefinitions>
								<StackPanel>
									<TextBlock Text="Pretension cutoff" FontWeight="SemiBold" />
									<TextBlock Style="{StaticResource DescriptionStyle}" Text="Pretension is disabled at and below this road speed." />
								</StackPanel>
								<TextBox Grid.Column="1" Text="{Binding ConfigVm.PretensionSpeedCutoffKph, UpdateSourceTrigger=PropertyChanged}" Visibility="{Binding ConfigVm.UseMetricUnits, Converter={StaticResource BoolToVis}}" IsEnabled="{Binding ConfigVm.UseSpeedSensor}" />
								<TextBox Grid.Column="1" Text="{Binding ConfigVm.PretensionSpeedCutoffMph, UpdateSourceTrigger=PropertyChanged}" Visibility="{Binding ConfigVm.UseImperialUnits, Converter={StaticResource BoolToVis}}" IsEnabled="{Binding ConfigVm.UseSpeedSensor}" />
							</Grid>

							<Grid Margin="0 0 0 14">
								<Grid.ColumnDefinitions>
									<ColumnDefinition Width="*" />
									<ColumnDefinition Width="150" />
								</Grid.ColumnDefinitions>
								<StackPanel>
									<TextBlock Text="Temperature sensor" FontWeight="SemiBold" />
									<TextBlock Style="{StaticResource DescriptionStyle}" Text="Which temperature sensors are used for thermal limiting." />
								</StackPanel>
								<ComboBox Grid.Column="1" ItemsSource="{Binding ConfigVm.TemperatureSensorOptions}" SelectedItem="{Binding ConfigVm.UseTemperatureSensor, UpdateSourceTrigger=PropertyChanged}" />
							</Grid>

							<Grid>
								<Grid.ColumnDefinitions>
									<ColumnDefinition Width="*" />
									<ColumnDefinition Width="150" />
								</Grid.ColumnDefinitions>
								<StackPanel>
									<TextBlock Text="Lights mode" FontWeight="SemiBold" />
									<TextBlock Style="{StaticResource DescriptionStyle}" Text="How the controller handles the lights output." />
								</StackPanel>
								<ComboBox Grid.Column="1" ItemsSource="{Binding ConfigVm.LightsModeOptions}" SelectedItem="{Binding ConfigVm.LightsMode, UpdateSourceTrigger=PropertyChanged}" />
							</Grid>
						</StackPanel>
					</Border>

					<Border Style="{StaticResource SectionStyle}">
						<StackPanel>
							<TextBlock Text="Speed sensor and shifting" FontSize="18" FontWeight="SemiBold" Margin="0 0 0 14" />

							<Grid Margin="0 0 0 14">
								<Grid.ColumnDefinitions>
									<ColumnDefinition Width="*" />
									<ColumnDefinition Width="110" />
								</Grid.ColumnDefinitions>
								<StackPanel>
									<TextBlock Text="Wheel size" FontWeight="SemiBold" />
									<TextBlock Style="{StaticResource DescriptionStyle}" Text="Wheel diameter used for speed calculation." />
								</StackPanel>
								<TextBox Grid.Column="1" Text="{Binding ConfigVm.WheelSizeInch, UpdateSourceTrigger=LostFocus}" />
							</Grid>

							<Grid Margin="0 0 0 14">
								<Grid.ColumnDefinitions>
									<ColumnDefinition Width="*" />
									<ColumnDefinition Width="110" />
								</Grid.ColumnDefinitions>
								<StackPanel>
									<TextBlock Text="Sensor signals" FontWeight="SemiBold" />
									<TextBlock Style="{StaticResource DescriptionStyle}" Text="Speed sensor pulses per wheel rotation." />
								</StackPanel>
								<TextBox Grid.Column="1" Text="{Binding ConfigVm.SpeedSensorSignals, UpdateSourceTrigger=PropertyChanged}" />
							</Grid>

							<Grid Margin="0 0 0 14">
								<Grid.ColumnDefinitions>
									<ColumnDefinition Width="*" />
									<ColumnDefinition Width="110" />
								</Grid.ColumnDefinitions>
								<StackPanel>
									<TextBlock Text="Shift interrupt" FontWeight="SemiBold" />
									<TextBlock Style="{StaticResource DescriptionStyle}" Text="Power-cut duration when the shift sensor triggers." />
								</StackPanel>
								<TextBox Grid.Column="1" Text="{Binding ConfigVm.ShiftInterruptDuration, UpdateSourceTrigger=PropertyChanged}" IsEnabled="{Binding ConfigVm.IsShiftSensorSupported}" />
							</Grid>

							<Grid>
								<Grid.ColumnDefinitions>
									<ColumnDefinition Width="*" />
									<ColumnDefinition Width="110" />
								</Grid.ColumnDefinitions>
								<StackPanel>
									<TextBlock Text="Shift current limit" FontWeight="SemiBold" />
									<TextBlock Style="{StaticResource DescriptionStyle}" Text="Current threshold during shift interrupt, as a percent of max current." />
								</StackPanel>
								<TextBox Grid.Column="1" Text="{Binding ConfigVm.ShiftInterruptCurrentThresholdPercent, UpdateSourceTrigger=PropertyChanged}" IsEnabled="{Binding ConfigVm.IsShiftSensorSupported}" />
							</Grid>
						</StackPanel>
					</Border>
				</StackPanel>
			</Grid>
		</StackPanel>
	</ScrollViewer>
</UserControl>
