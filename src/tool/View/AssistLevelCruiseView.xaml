<UserControl x:Class="BBSFW.View.AssistLevelCruiseView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="450"
             d:DesignWidth="800">
	<StackPanel>
		<Grid Margin="0 0 0 14">
			<Grid.ColumnDefinitions>
				<ColumnDefinition Width="*" />
				<ColumnDefinition Width="120" />
			</Grid.ColumnDefinitions>
			<StackPanel>
				<TextBlock Text="Cruise current" FontWeight="SemiBold" />
				<TextBlock Text="Percent of global max current available to cruise mode."
						   Foreground="{DynamicResource MutedTextBrush}"
						   TextWrapping="Wrap"
						   Margin="0 3 18 0" />
			</StackPanel>
			<TextBox Grid.Column="1" Text="{Binding TargetCurrentPercent, UpdateSourceTrigger=PropertyChanged}" />
		</Grid>

		<Grid Margin="0 0 0 14">
			<Grid.ColumnDefinitions>
				<ColumnDefinition Width="*" />
				<ColumnDefinition Width="120" />
			</Grid.ColumnDefinitions>
			<StackPanel>
				<TextBlock Text="Cadence limit" FontWeight="SemiBold" />
				<TextBlock Text="Cadence ceiling while cruise is active."
						   Foreground="{DynamicResource MutedTextBrush}"
						   TextWrapping="Wrap"
						   Margin="0 3 18 0" />
			</StackPanel>
			<TextBox Grid.Column="1" Text="{Binding MaxCadencePercent, UpdateSourceTrigger=PropertyChanged}" />
		</Grid>

		<Grid>
			<Grid.ColumnDefinitions>
				<ColumnDefinition Width="*" />
				<ColumnDefinition Width="120" />
			</Grid.ColumnDefinitions>
			<StackPanel>
				<TextBlock Text="Speed limit" FontWeight="SemiBold" />
				<TextBlock Text="Percent of global max speed allowed in cruise mode."
						   Foreground="{DynamicResource MutedTextBrush}"
						   TextWrapping="Wrap"
						   Margin="0 3 18 0" />
			</StackPanel>
			<TextBox Grid.Column="1" Text="{Binding MaxSpeedPercent, UpdateSourceTrigger=PropertyChanged}" />
		</Grid>
	</StackPanel>
</UserControl>
